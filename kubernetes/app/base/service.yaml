apiVersion: v1
kind: Service
metadata:
  name: blueprint-app-svc
  labels:
    app: blueprint-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: blueprint-app
